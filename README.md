## «Разработано в Нижневартовском социально-гуманитарном колледже»

# AurСад - Детектор свежести яблок с AI

🍎 **AurСад** (ФруктAIметр) — мобильное приложение для ОС Аврора, определяющее свежесть яблок с помощью искусственного интеллекта и компьютерного зрения. Приложение работает полностью локально, без подключения к интернету, выполняя все вычисления на процессоре устройства.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Установка и сборка](#установка-и-сборка)
- [Использование](#использование)
- [Архитектура](#архитектура)
- [Разработка](#разработка)
- [Известные проблемы](#известные-проблемы)
- [Полезные ссылки](#полезные-ссылки)
- [Лицензия](#лицензия)

## 📖 Описание

AurСад — референс-приложение, демонстрирующее возможности ОС Аврора в области машинного обучения и обработки изображений. Приложение использует локальное обучение ML-модели на устройстве с помощью MLPack и выполняет классификацию яблок на три категории: хорошее, плохое или не яблоко.

### Основные сценарии использования

1. **Анализ качества яблок** — определение свежести яблок по фотографии
2. **Локальное обучение** — обучение ML-модели на устройстве без подключения к интернету
3. **Работа с камерой** — захват и анализ изображений в реальном времени
4. **Сегментация** — использование polygon данных из LabelMe для точного анализа

## ✨ Возможности

### Реализованные функции

- 🔍 **Определение качества яблок** — классификация на три класса: хорошее / плохое / не яблоко
- 🎓 **Локальное обучение ML-модели** — обучение на устройстве с помощью MLPack
- 📱 **Работа без интернета** — все вычисления выполняются локально
- 🖼️ **Обработка изображений** — извлечение признаков (цвет, текстура, форма)
- 📊 **Показатель уверенности** — отображение уверенности модели (0-100%)
- 📷 **Работа с камерой** — захват изображений и анализ в реальном времени
- 🎯 **Сегментация** — использование polygon данных из LabelMe для точного анализа
- 💾 **Сохранение модели** — сохранение и загрузка обученной модели

## 🛠️ Технологический стек

### Основные технологии

- **Язык программирования**: C++11
- **UI Framework**: Qt 5 + QML (Sailfish Silica)
- **Платформа**: ОС Аврора 5.1.6+
- **Управление зависимостями**: Conan 2.x

### Библиотеки

- **Обработка изображений**: OpenCV 4.10.0 (планируется)
- **Машинное обучение**: MLPack 4.4.0 (планируется)
- **Мультимедиа**: Qt Multimedia (камера)

### Инструменты сборки

- **Build System**: qmake (Qt)
- **Package Manager**: mb2 (MerSDK Build tool 2)
- **Package Format**: RPM

## 📁 Структура проекта

```
aurcad/
├── src/                          # Исходный код C++
│   ├── main.cpp                  # Точка входа приложения
│   ├── AppleDetector.{h,cpp}     # Главный класс детектора (QObject)
│   ├── ImageProcessor.{h,cpp}    # Обработка изображений и извлечение признаков
│   ├── ImageProcessorExtended.cpp # Расширенные методы обработки
│   ├── AppleClassifier.{h,cpp}   # ML классификация (MLPack)
│   ├── CameraHandler.{h,cpp}     # Управление камерой устройства
│   └── SegmentationData.{h,cpp}  # Парсинг LabelMe аннотаций
├── qml/                          # QML интерфейс
│   ├── aurcad.qml                # Главное окно приложения
│   ├── pages/
│   │   ├── MainPage.qml          # Главная страница с анализом
│   │   ├── TrainingPage.qml      # Страница обучения модели
│   │   ├── CameraPage.qml        # Страница работы с камерой
│   │   └── AboutPage.qml         # О приложении
│   └── cover/
│       └── DefaultCoverPage.qml  # Обложка приложения
├── icons/                        # Иконки приложения
│   ├── 86x86/
│   ├── 108x108/
│   ├── 128x128/
│   └── 172x172/
├── omsk/                         # Датасет
│   ├── Training/                 # Изображения яблок (57 JPG)
│   ├── labelme/                  # Аннотации сегментации (57 JSON)
│   └── classes.txt               # Метки классов (хорошее/плохое)
├── dataset_prepared/             # Подготовленный датасет
│   └── dataset_info.json        # Метаданные датасета
├── tools/                        # Утилиты
│   └── prepare_dataset.py        # Скрипт подготовки датасета
├── translations/                 # Файлы переводов
│   ├── ru.auroraos.aurcad.ts
│   └── ru.auroraos.aurcad-ru.ts
├── rpm/                          # RPM спецификация
│   └── ru.auroraos.aurcad.spec
├── conanfile.txt                 # Зависимости Conan
├── ru.auroraos.aurcad.pro        # Qt project файл
├── ru.auroraos.aurcad.desktop    # Desktop Entry файл
├── resources.qrc                 # Qt ресурсы
├── README.md                     # Этот файл
└── PROJECT_SUMMARY.md            # Итоговая сводка проекта
```

## 🚀 Установка и сборка

### Предварительные требования

1. **Aurora SDK 5.1.6.110+**
   - Скачайте с [developer.auroraos.ru](https://developer.auroraos.ru)
   - Установите согласно инструкциям

2. **Conan Package Manager** (опционально)
   - Обычно уже установлен в Aurora SDK
   - Используется для управления ML-библиотеками

### Быстрая сборка

```bash
# 1. Клонируйте репозиторий
git clone < ссылка на репозиторий >
cd aurcad

# 2. Подключитесь к Aurora Build Engine через SSH
ssh -p 2222 -i C:/AuroraOS/vmshare/ssh/private_keys/sdk mersdk@localhost

# 3. В SSH-сессии Build Engine
cd ~/aurcad  # или путь к вашему проекту

# 4. Инициализация и сборка
mb2 build-init
mb2 build
mb2 rpm

# 5. Установка на эмулятор
mb2 deploy-rpm
```

### Детальная инструкция

Подробные инструкции по сборке, включая настройку зависимостей, решение проблем и альтернативные способы установки, см. в [BUILD.md](BUILD.md).

### Установка зависимостей (опционально)

```bash
# В SSH-сессии Build Engine, внутри sb2 sdk-install
sb2 -t AuroraOS-5.1.6.110-MB2-x86_64 -m sdk-install -R

# Настройка Conan remotes
conan remote add omprussiapublic https://releases.omprussia.ru/conan/stable || true
conan remote add conancenter https://center.conan.io || true

# Установка зависимостей
cd ~/aurcad
conan install . -s build_type=Release --build=missing

exit
```

**Примечание**: Если пакеты OpenCV и MLPack недоступны, приложение будет работать с базовой функциональностью.

## 📖 Использование

### 1. Обучение модели

При первом запуске необходимо обучить модель:

1. Откройте приложение **AurСад**
2. Откройте меню (pull down) → **"Обучить модель"**
3. Нажмите **"Начать обучение"**
4. Дождитесь завершения обучения (~1-2 минуты)
5. Модель будет сохранена автоматически

### 2. Анализ яблока

1. На главной странице нажмите **"Выбрать фото"**
2. Выберите изображение яблока из датасета или с камеры
3. Нажмите **"Анализировать"**
4. Получите результат:
   - 🟢 **Хорошее** — свежее яблоко
   - 🔴 **Плохое** — испорченное яблоко
   - 🟠 **Не яблоко** — на фото не яблоко

### 3. Работа с камерой

1. На главной странице нажмите **"Камера"**
2. Разрешите доступ к камере
3. Наведите камеру на яблоко
4. Нажмите кнопку захвата или включите режим непрерывной детекции
5. Получите результат анализа в реальном времени

### 4. Использование polygon данных

1. Включите опцию **"Использовать polygon данные"** в настройках
2. При обучении модель будет использовать только область яблока (игнорируя фон)
3. Это повышает точность классификации

## 🏗️ Архитектура

### Общая архитектура

Приложение построено по многослойной архитектуре:

```
┌─────────────────────────────┐
│      QML UI Layer           │  ← Пользовательский интерфейс
└──────────┬──────────────────┘
           │ Signals/Slots
┌──────────▼──────────────────┐
│   AppleDetector (QObject)   │  ← Координатор (фасад для QML)
└──────────┬──────────────────┘
           │
     ┌─────┴─────┐
     ▼           ▼
┌─────────┐ ┌──────────┐
│  Image  │ │ Classifier│      ← Логика обработки
│Processor│ │ (MLPack) │
└─────────┘ └──────────┘
     │           │
     ▼           ▼
┌─────────┐ ┌──────────┐
│ OpenCV  │ │  MLPack  │       ← Библиотеки ML
└─────────┘ └──────────┘
```

### Основные компоненты

#### AppleDetector
- **Роль**: Фасад для QML, координатор работы
- **Функции**: Управление workflow, связь между UI и логикой
- **Signals/Slots**: Коммуникация с QML через Qt signals/slots

#### ImageProcessor
- **Роль**: Обработка изображений и извлечение признаков
- **Функции**:
  - Извлечение цветовых признаков (RGB, HSV гистограммы)
  - Извлечение текстурных признаков (edges, contrast)
  - Извлечение признаков формы (при использовании polygon данных)
  - Детекция яблок (подготовлено для YOLO11)

#### AppleClassifier
- **Роль**: ML классификация с использованием MLPack
- **Алгоритмы**: Random Forest / Logistic Regression
- **Классы**: GOOD (0), BAD (1), NOT_APPLE (2)

#### CameraHandler
- **Роль**: Управление камерой устройства
- **Функции**: Захват изображений, real-time видеопоток

#### SegmentationData
- **Роль**: Парсинг LabelMe аннотаций
- **Функции**: Извлечение polygon данных, создание масок

### Поток данных

#### Анализ изображения

```
Изображение → ImageProcessor::detectApple()
           → ImageProcessor::extractFeatures()
           → AppleClassifier::predict()
           → Результат (класс + уверенность)
```

#### Обучение модели

```
Датасет → Извлечение признаков для каждого изображения
       → Разделение на train/test (80/20)
       → Обучение Random Forest
       → Валидация
       → Сохранение модели
```

Подробное описание архитектуры см. в [ARCHITECTURE.md](ARCHITECTURE.md).

## 🔧 Разработка

### Добавление новых признаков

Отредактируйте `src/ImageProcessor.cpp`:

```cpp
std::vector<double> ImageProcessor::extractFeatures(const QString &imagePath)
{
    // Добавьте свои признаки здесь
    // Например: HOG, SIFT, deep features
}
```

### Изменение ML алгоритма

Отредактируйте `src/AppleClassifier.cpp`:

```cpp
float AppleClassifier::train(...)
{
    // Замените Random Forest на другой алгоритм
    // Например: SVM, Neural Network, Gradient Boosting
}
```

### Добавление YOLO детекции

1. Установите YOLO11 модель
2. В `ImageProcessor.cpp`:

```cpp
bool ImageProcessor::detectApple(const QString &imagePath)
{
    // Загрузите YOLO11 модель
    // Запустите детекцию
    // Проверьте класс "apple"
    // return true если найдено
}
```

### Отладка

```bash
# Запуск с логами
QT_LOGGING_RULES="*.debug=true" ru.auroraos.aurcad

# Просмотр логов
journalctl -f | grep aurcad

# Отладка через GDB
gdb /usr/bin/ru.auroraos.aurcad
```

### Подготовка датасета

```bash
# Запустите скрипт подготовки
python3 tools/prepare_dataset.py

# Проверьте результаты
cat dataset_prepared/dataset_info.json
```

## 📊 Датасет

### Структура датасета

- **Источник**: `omsk/` директория
- **Размер**: 57 изображений яблок (JPG)
- **Разметка**:
  - `classes.txt` — метки классов (хорошее/плохое)
  - `labelme/` — polygon сегментация яблок (JSON)
- **Соотношение**: ~60% хорошие, ~40% плохие

### Формат данных

**classes.txt**:
```
1.jpg хорошее
5.jpg хорошее
6.jpg плохое
...
```

**LabelMe JSON**:
```json
{
  "version": "5.9.1",
  "shapes": [{
    "label": "apple",
    "points": [[x1, y1], [x2, y2], ...],
    "shape_type": "polygon"
  }],
  "imagePath": "Training/1.jpg"
}
```

## 🐛 Известные проблемы

1. **OpenCV и MLPack**: В текущей версии используются заглушки. После настройки Conan зависимостей раскомментируйте соответствующие `#include` в исходниках.

2. **Иконки**: Убедитесь, что иконки приложения созданы в папках `icons/{86x86,108x108,128x128,172x172}/ru.auroraos.aurcad.png`

3. **Датасет**: Убедитесь, что путь к датасету корректен. По умолчанию используется `/home/user/aurcad/omsk/Training`

4. **Conan зависимости**: Если пакеты OpenCV и MLPack недоступны в репозиториях, приложение будет работать с базовой функциональностью.

## 💡 Советы

- Для лучших результатов используйте четкие фотографии яблок на однородном фоне
- Обучите модель несколько раз для улучшения качества
- Используйте polygon режим для повышения точности (игнорирует фон)
- Сохраняйте обученную модель через меню обучения
- При изменении датасета переобучите модель

## 📚 Полезные ссылки

### Документация

- [Документация Aurora OS](https://developer.auroraos.ru)
- [Видеокурс по разработке](https://stepik.org/course/71788/syllabus)
- [Conan для Aurora](https://developer.auroraos.ru/doc/software_development/guides/conan/conan)

## 🎯 TODO / Планы развития

- [ ] Экспорт результатов в CSV/JSON
- [ ] Визуализация сегментации на изображении
- [ ] Поддержка других фруктов
- [ ] Метрики качества модели (Precision, Recall, F1)
- [ ] Дообучение модели новыми примерами

## 📄 Лицензия

BSD-3-Clause